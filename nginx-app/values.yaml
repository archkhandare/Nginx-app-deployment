# Default values for my-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: archanaadmin02/my_nginximage:v1
  pullPolicy: IfNotPresent
  tag: "v1"

service:
   type: NodePort
   port: 80
